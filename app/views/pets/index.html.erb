<h1>Pets</h1>

<% @pets.each do |pet| %>
  <img src="#{pet.image}">
  <h3><a href="/pets/<%= pet.id %>"><%= pet.name %></a></h3>
  <h3><%= pet.age %></h3>
  <h3><%= pet.sex %></h3>
  <h3><%= pet.shelter.name %></h3>
<% end %>

<%=link_to 'New Pet', '/pets/new' %>
